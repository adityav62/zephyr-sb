/ {
    chosen {
        zephyr,console = &uart0;
    };
};

//Any changes to UART configuration must be done here, not to the board's .dtsi
  
&uart0 {
    status = "okay";
    pinctrl-0 = <&uart0_default>;
    pinctrl-names = "default";
    current-speed = <115200>;
};
